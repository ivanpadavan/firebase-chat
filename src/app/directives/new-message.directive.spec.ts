import { NewMessageDirective } from './new-message.directive';

describe('NewMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new NewMessageDirective();
    expect(directive).toBeTruthy();
  });
});
